[package]
name = "memory-local"
edition = "2024"
version = "0.1.0"
publish = false

[dependencies]
tokio = { version = "1.51.0", default-features = false, features = ["time"] }
dial9-tokio-telemetry = { path = "../../dial9-tokio-telemetry", features = [
    "memory-profiling",
] }
